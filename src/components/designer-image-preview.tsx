/* eslint-disable @next/next/no-img-element -- External Webflow assets are previewed without proxying customer images. */
export function DesignerImagePreview({before,after,afterSrc,beforeLabel,afterLabel,emptyLabel}:{before:string;after:string;afterSrc?:string;beforeLabel:string;afterLabel:string;emptyLabel?:string}){
 return <div className="diff-grid grid gap-4 sm:grid-cols-2"><div><small>{beforeLabel}</small><img src={before} alt={beforeLabel} referrerPolicy="no-referrer" className="image-diff-preview" style={{width:"100%",height:150,objectFit:"contain"}}/><p className="image-url break-all text-xs">{before}</p></div><div className="diff-after"><small>{afterLabel}</small>{after?<><img src={afterSrc??after} alt={afterLabel} referrerPolicy="no-referrer" className="image-diff-preview" style={{width:"100%",height:150,objectFit:"contain"}}/><p className="image-url break-all text-xs">{after}</p></>:<p className="image-preview-empty" style={{minHeight:150,display:"flex",alignItems:"center"}}>{emptyLabel}</p>}</div></div>;
}
